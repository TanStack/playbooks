Implement `client.mjs` using the installed `@intent-fixture/parcel-client`. Export `fetchProfile(api, { signal } = {})` to call `api.getProfile()`, retry `E_TEMP` failures up to three total calls, preserve permanent errors and cancellation. Export `collectAll(fetchPage)` to collect every item in order, including empty intermediate pages and empty-string cursors, and propagate pagination errors. Only create `client.mjs`; do not modify installed packages. Do not commit or push.
